GFG
find peak element in array : left right compare
maximum-and-minimum-in-an-array : 
Reverse an Array : array.reverse()
Sort The Array : arrays.sort()
Kth smallest element :  Sort the given array &     return arr[k - 1];





'input' is an array or not. > (toString.call(input) === "[object Array]")
Clone an array >    arra1.slice(0);
                    or spread operator (...), > y = [...x];
                    or y = Array.from(x);
                    or y = x.slice();
                    or y = x.map(i => i);
                    or y = x.filter(() => true);
                    

